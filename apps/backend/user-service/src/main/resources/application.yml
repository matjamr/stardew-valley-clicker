spring:
  cloud:
    aws:
      dynamodb:
        region: local
      credentials:
        access-key: fake
        secret-key: fake
  grpc:
    server:
      reflection:
        enabled: true
      ssl:
        secure: false
  data:
    redis:
      port: 6379
      host: localhost

aws:
  region: local
  dynamodb:
    endpoint: http://localhost:8000
    accessKey: fake
    secretKey: fake
  dynamo:
    sessionToken: fake-token